# Test configuration for Datanode with S3 storage (non-existent bucket)
node_id = 2

[meta_client]
metasrv_addrs = ["127.0.0.1:3002"]
timeout = "3s"

[storage]
type = "S3"
bucket = "non-existent-bucket"
root = "test-data"
access_key_id = "minioadmin"
secret_access_key = "minioadmin"
endpoint = "http://localhost:9000"
region = "us-east-1"
