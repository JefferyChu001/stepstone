# Test configuration for Datanode with S3 storage (readonly user)
node_id = 2

[meta_client]
metasrv_addrs = ["127.0.0.1:3002"]
timeout = "3s"

[storage]
type = "S3"
bucket = "greptime-test-bucket"
root = "test-data"
access_key_id = "readonly_user"
secret_access_key = "readonly_password"
endpoint = "http://localhost:9000"
region = "us-east-1"
